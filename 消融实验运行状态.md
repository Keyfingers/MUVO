# 🔬 消融实验运行状态

## ✅ 实验已启动

**启动时间**: 2024-10-31 10:13  
**运行方式**: 后台运行（nohup）  
**预计完成时间**: 约3-4小时后（14:00-15:00）

---

## 📊 实验进度

实验将按顺序运行：
1. ✅ **Image-Only** (仅图像分支) - 正在运行中
2. ⏳ **LiDAR-Only** (仅点云分支) - 等待中
3. ⏳ **Late Fusion** (后期融合) - 等待中
4. ⏳ **Dynamic-Only** (仅异常数据训练) - 等待中
5. ⏳ **Lightweight** (轻量级模型) - 等待中

---

## 🔍 监控命令

### 查看总体进度
```bash
tail -f ablation_full_run.log
```

### 查看当前实验详细日志
```bash
# 实验1: Image-Only
tail -f ablation_results/logs/image_only.log

# 实验2: LiDAR-Only (完成后可用)
tail -f ablation_results/logs/lidar_only.log

# 实验3: Late Fusion (完成后可用)
tail -f ablation_results/logs/late_fusion.log

# 实验4: Dynamic-Only (完成后可用)
tail -f ablation_results/logs/dynamic_only.log

# 实验5: Lightweight (完成后可用)
tail -f ablation_results/logs/lightweight.log
```

### 查看GPU使用情况
```bash
watch -n 2 nvidia-smi
```

### 查看进程状态
```bash
ps aux | grep ablation
```

---

## 📂 结果文件位置

### 单个实验结果
```
ablation_results/
├── image_only/
│   ├── best_model.pth      # 最佳模型权重
│   ├── history.npz         # 训练历史
│   └── report.json         # 性能报告
├── lidar_only/
│   └── ...
├── late_fusion/
│   └── ...
├── dynamic_only/
│   └── ...
└── lightweight/
    └── ...
```

### 对比分析结果（实验完成后自动生成）
```
ablation_results/comparison/
├── performance_comparison.png/pdf      # 性能对比图
├── confusion_matrices_comparison.png/pdf  # 混淆矩阵对比
├── training_curves_comparison.png/pdf    # 训练曲线对比
├── param_performance_tradeoff.png/pdf    # 参数-性能权衡图
├── ablation_table.tex                   # LaTeX表格（论文用）
├── comparison_table.csv                 # CSV表格
└── analysis_report.txt                  # 详细分析报告
```

---

## ⏱️ 时间估算

根据您的训练速度（30 epochs约50分钟）：

| 实验 | 预计时间 | 说明 |
|------|---------|------|
| Image-Only | ~25分钟 | 仅图像分支，较轻量 |
| LiDAR-Only | ~20分钟 | 仅点云分支，最轻量 |
| Late Fusion | ~35分钟 | 两个分支，最重 |
| Dynamic-Only | ~30分钟 | 可能提前失败（预期） |
| Lightweight | ~15分钟 | 通道数减半，最快 |
| **总计** | **~2.5小时** | 加上对比分析约3-4小时 |

---

## 🎯 预期结果

基于完整模型的优异性能（Recall 99.74%），预期：

| 配置 | 预期 Val Recall | 说明 |
|-----|----------------|------|
| **Full Model** | **99.74%** | 基准（已完成） |
| Image-Only | ~94-96% | 缺少3D空间信息 |
| LiDAR-Only | ~92-94% | 缺少纹理和颜色 |
| Late Fusion | ~97-98% | 缺少深层特征交互 |
| Dynamic-Only | ❌ 失败 | 无法学习"正常" |
| Lightweight | ~95-97% | 模型容量不足 |

---

## 📝 论文使用建议

实验完成后，可以使用生成的文件：

1. **表格**: `ablation_results/comparison/ablation_table.tex`
2. **图表**: `ablation_results/comparison/*.pdf`
3. **分析**: `ablation_results/comparison/analysis_report.txt`

---

## 🛑 如果需要停止

```bash
# 停止所有消融实验
pkill -f ablation_study

# 或停止整个脚本
pkill -f run_all_ablations
```

---

## ✅ 实验完成后

所有实验完成后，会自动：
1. ✅ 生成对比图表（PNG + PDF）
2. ✅ 生成LaTeX表格
3. ✅ 生成详细分析报告
4. ✅ 保存CSV数据表格

然后您可以：
```bash
# 查看最终对比报告
cat ablation_results/comparison/analysis_report.txt

# 查看生成的图表
ls -lh ablation_results/comparison/*.png
```

---

**🎉 祝实验顺利！如有问题随时询问Spark大人！**




